# 2025A 问题 2 验证报告

## 结论

在严格整圆柱遮蔽、$g=9.8\ \mathrm{m/s^2}$ 的已冻结口径下，当前最佳已知策略为立即投放、以最低允许速度飞行：

$$
(\theta,v,t^{\mathrm r},\delta)
=(3.0829669145,70,0,2.4967856396).
$$

其严格遮蔽区间为

$$
[2.4967876895,7.0396676367]\ \mathrm s,
$$

总时长为 $4.5428799472$ s，正文建议报告为 **4.543 s**。

## 策略输出

| 项目 | 数值 |
|---|---:|
| 航向角 | $3.0829669145$ rad，$176.6409926^\circ$ |
| 飞行速度 | $70.0000$ m/s |
| 投放时刻 | $0$ s |
| 投放点 | $(17800,0,1800)$ m |
| 引信时长 | $2.4967856396$ s |
| 起爆时刻 | $2.4967856396$ s |
| 起爆点 | $(17625.5252681,10.2404449,1769.4537012)$ m |
| 严格遮蔽时长 | $4.5428799472$ s |
| 中心视线对照 | $4.5726737614$ s |

全部速度、时间和起爆高度约束均满足。云团在计算窗内的最低中心高度为 $1709.4537$ m，没有触发地面处理分歧。

## 搜索审计

问题 2 的有效策略在四维盒约束内十分稀疏。未经引导的随机种群会整体落入零遮蔽平台，因此求解流程采用：已知可行基准注入、零时长几何裕量引导、中心视线候选搜索、严格判据全局搜索、多起点局部精修和正式精度统一排名。

四维搜索发现最佳区域位于 $v=70$、$t^{\mathrm r}=0$ 两个下界。随后以不同随机种子 731 固定这两个边界，独立优化 $\theta$ 和 $\delta$，得到最终候选。全局与部分局部优化阶段因预算上限停止，故日志明确标为 `budget-exhausted-with-feasible-candidates`，没有声明全局收敛。

## 数值收敛

| 时间扫描步长（s） | 每条圆周初值数 | 严格时长（s） |
|---:|---:|---:|
| 0.10 | 256 | 4.542879947227982 |
| 0.05 | 512 | 4.542879947235083 |
| 0.02 | 1024 | 4.542879947227929 |
| 0.02 | 2048 | 4.542879947227961 |

四组结果最大差为 $7.2\times10^{-12}$ s，远低于论文报告精度。

## 时间边界检查

最优策略在起爆后约 $2.05\times10^{-6}$ s 开始严格遮蔽，因此边界检查将“起爆前未激活”和“起爆后但尚未遮蔽”分开：

- 起爆前：云团未激活，遮蔽为假；
- 起爆后、区间起点前：严格裕量为正，遮蔽为假；
- 区间起点：严格裕量在 $10^{-12}$ m 量级，判为边界；
- 区间内部：严格裕量为负；
- 区间终点：严格裕量在 $10^{-10}$ m 以内，判为边界；
- 区间终点后：严格裕量恢复为正。

## 独立几何检查

独立检查不调用切锥裕量公式，而是在圆柱侧面和上下底面共采样 88,560 点，直接计算云团中心到每条导弹—目标点视线线段的距离。起爆前、遮蔽开始前、起点、内部、终点和终点后的布尔序列为：

```text
假、假、真、真、真、假
```

与主切锥实现一致。

## 局部扰动与回归

对航向角作正负 $10^{-4}$ rad 扰动、速度增加 0.1 m/s、投放延迟 0.01 s、引信时长作正负 0.01 s 扰动，六个候选的严格时长均未超过当前策略。由于速度和投放时刻已经位于允许下界，不能对其作负向可行扰动。

将问题 1 策略输入问题 2 的通用内核，所得严格时长与已验收问题 1 结果仅差 $1.78\times10^{-15}$ s，说明泛化实现没有破坏基准行为。

## 适用性声明

本报告支持以下结论：策略可行，严格遮蔽时长可重复计算，时间与几何离散已经收敛，边界及独立视线实现一致，候选在所测局部扰动下稳定。由于有限预算的数值搜索不能构成全局最优证明，论文中应使用“优化得到的最佳策略”或“当前最佳已知策略”，不应写成“严格证明的全局最优解”。

机器可读证据见 `outputs/logs/q2_optimization.json` 与 `outputs/logs/q2_validation.json`，输入哈希、种子、软件版本和产物路径见 `outputs/manifest.json`。
