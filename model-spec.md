# 2025A 模型规格

## 0. 当前状态

- 建模阶段：问题 4——FY1、FY2、FY3 三机各一弹联合覆盖优化。
- 规格状态：**问题 1、2 已验收；问题 3 形式化已冻结，正在实现**。
- 已冻结口径：严格整目标遮蔽为主判据，圆柱中心视线近似作为对照。
- 权威输入：`data/raw/CUMCM-2025-problem+A-Chinese.pdf`。
- 本轮范围：完成问题 4；验收后进入问题 5。

## 1. 建模契约

### 1.1 现实问题与用途

建立一个可重复计算的几何—运动学模型，判断给定烟幕弹在某一时间是否阻断来袭导弹对真目标的视线，并据此计算有效遮蔽时间。问题 1 用作独立基准；其几何判据和时间积分约定一旦通过检查，再作为问题 2–5 的共同计算内核。

### 1.2 分析单位、边界与时域

- 分析单位：一枚导弹—一枚烟幕弹—真目标之间随时间变化的视线关系。
- 空间边界：题面定义的三维直角坐标系，单位统一为 m。
- 时间原点：雷达发现导弹且控制中心下达任务的时刻，记为 $t=0$，单位为 s。
- 单枚导弹时域：$0\le t\le t_j^{\mathrm{hit}}$，其中导弹到达假目标原点即结束建模。
- 单枚云团有效时域：$t_{ik}^{\mathrm e}\le t\le t_{ik}^{\mathrm e}+20$；端点是否闭合不影响连续时长。
- 不建模：空气阻力、风、无人机加速度阶段、爆炸持续过程、云团扩散和浓度空间渐变、导弹制导反馈、导弹或无人机碰撞规避。

### 1.3 必须输出

问题 1 最终至少输出：

1. 投放点与起爆点；
2. 有效遮蔽时间区间（可不止一个）；
3. 区间并集的总长度，单位 s；
4. 使用的遮蔽判据、重力加速度、数值容差和时间求根误差；
5. 可独立复算的配置与运行记录。

问题 2–5 的策略输出格式待问题 1 基准完成后逐问细化；题面所述三个 Excel 模板目前缺失，不能确认最终字段。

### 1.4 成功标准

- 相同输入、判据和容差产生相同结果。
- 几何判断只在“导弹—真目标”视线线段上成立，不能把导弹后方或目标后方的烟幕误算为遮蔽。
- 有效时长通过区间边界求解与更细时间网格交叉检查，而不是仅用网格点数乘步长。
- 问题 1 的结果通过至少一个独立实现或手算极限情形复核后，才进入问题 2。

## 2. 事实、假设与未知量台账

| 编号 | 内容 | 类型 | 状态/来源 |
|---|---|---|---|
| F1 | 假目标为原点，$xy$ 为水平面 | 题面事实 | PDF 第 1 页 |
| F2 | 真目标为半径 7 m、高 10 m 的固定圆柱，下底圆心 $(0,200,0)$ | 题面事实 | PDF 第 1 页 |
| F3 | 导弹速度 300 m/s，方向直指假目标 | 题面事实 | PDF 第 1 页 |
| F4 | 无人机受领任务后可瞬时转向，随后以 70–140 m/s 等高度匀速直线飞行，航向和速度不再改变 | 题面事实 | PDF 第 1 页 |
| F5 | 弹体脱离无人机后仅受重力作用 | 题面事实 | PDF 第 1 页 |
| F6 | 起爆瞬时形成半径 10 m 的有效球形区域；起爆后 20 s 内有效 | 题面事实 | PDF 第 1 页；“中心 10 m 范围内”形式化为闭球 |
| F7 | 云团以 3 m/s 匀速下沉 | 题面事实 | PDF 第 1 页 |
| F8 | 同一无人机相邻两次投弹至少间隔 1 s | 题面事实 | PDF 第 1 页 |
| A1 | 弹体投放瞬间继承无人机的水平速度，初始竖直速度为 0 | 建模假设 | 由“脱离后在重力作用下运动”作出的标准抛体解释；需记录 |
| A2 | 起爆后云团水平位置不变，只沿 $-z$ 方向移动 | 建模假设 | “以 3 m/s 下沉”的最简无风解释 |
| A3 | 地面为 $z=0$，弹体应在起爆前保持 $z\ge0$ | 建模假设 | 物理可行性约束，题面未说明触地行为 |
| A4 | 导弹到达假目标原点后不再参与遮蔽计时 | 建模假设 | 模型时域边界 |
| D1 | “有效遮蔽”采用严格整目标遮蔽；圆柱中心视线近似仅作对照 | 用户决定 | 2026-08-20 已确认 |
| D2 | 主计算采用 $g=9.8\ \mathrm{m/s^2}$，以 $9.81$ 作敏感性核对 | 用户决定 | 2026-08-20 已确认 |
| D3 | 云团在起爆后 20 s 内按 3 m/s 继续下沉，不因中心低于地面而在主模型中提前失效；若最优解触发地下情形则补做截断敏感性 | 用户决定 | 2026-08-20 已确认 |
| D4 | 多云团采用联合视线覆盖：每条目标视线只需被至少一个有效云团阻断；“某个单云独立遮住整个圆柱”的区间并集仅作保守对照 | 用户决定 | 2026-08-20 已确认 |
| U3 | 云团中心降到地面以下时如何处理 | 未解决未知 | 问题 1 不会触发；优化阶段可能触发 |
| U4 | 问题 5 的多导弹目标是总遮蔽时长、各导弹时长之和、最短时长最大化或其他复合目标 | 未解决未知 | 到问题 5 前确认 |
| U5 | `result1.xlsx`、`result2.xlsx`、`result3.xlsx` 的字段和格式 | 缺失输入 | 当前项目目录无附件模板 |

## 3. 坐标、集合与符号

### 3.1 固定集合

- 导弹索引 $j\in\mathcal J=\{1,2,3\}$。
- 无人机索引 $i\in\mathcal I=\{1,\ldots,5\}$。
- 无人机 $i$ 投放的烟幕弹索引 $k\in\mathcal K_i$。
- 假目标：$O=(0,0,0)$。
- 真目标实体：

$$
\mathcal T=\{(x,y,z)\in\mathbb R^3:x^2+(y-200)^2\le 7^2,\ 0\le z\le10\}.
$$

若采用代表点近似，建议代表点为圆柱几何中心

$$
T_c=(0,200,5),
$$

而不是题面给出的下底圆心。该点仅用于用户确认的中心视线对照，不参与严格判据。

### 3.2 参数

| 符号 | 含义 | 单位/范围 |
|---|---|---|
| $M_{j0}$ | 导弹 $j$ 在 $t=0$ 的位置 | m，题面给定 |
| $U_{i0}$ | 无人机 $i$ 在 $t=0$ 的位置 | m，题面给定 |
| $v_M$ | 导弹速度 | $300\ \mathrm{m/s}$ |
| $v_i$ | 无人机 $i$ 的速度 | $[70,140]\ \mathrm{m/s}$ |
| $\theta_i$ | 无人机水平航向角，从 $+x$ 轴逆时针计 | rad，$[0,2\pi)$ |
| $g$ | 重力加速度 | 主值 $9.8$，敏感性值 $9.81\ \mathrm{m/s^2}$ |
| $R$ | 云团有效半径 | $10\ \mathrm m$ |
| $v_C$ | 云团下沉速度 | $3\ \mathrm{m/s}$ |
| $T_C$ | 单个云团有效寿命 | $20\ \mathrm s$ |
| $t_{ik}^{\mathrm r}$ | 投放时刻 | s，$\ge0$ |
| $\delta_{ik}$ | 投放至起爆的引信时长 | s，$\ge0$ |
| $t_{ik}^{\mathrm e}$ | 起爆时刻 | $t_{ik}^{\mathrm r}+\delta_{ik}$ |

### 3.3 变量类型

- 问题 1 中 $v_1,\theta_1,t_{11}^{\mathrm r},\delta_{11}$ 均为题面固定输入。
- 问题 2 起，$v_i,\theta_i,t_{ik}^{\mathrm r},\delta_{ik}$ 才是决策变量。
- $M_j(t)$、$U_i(t)$、$B_{ik}(t)$、$C_{ik}(t)$ 分别为导弹、无人机、弹体、云团中心的状态变量。
- 遮蔽指示量是由连续状态派生的布尔量，不是独立决策变量。

## 4. 共用运动学模型

### 4.1 导弹轨迹

令

$$
\hat d_j=-\frac{M_{j0}}{\|M_{j0}\|_2},
$$

则

$$
M_j(t)=M_{j0}+v_Mt\hat d_j
=\left(1-\frac{v_Mt}{\|M_{j0}\|_2}\right)M_{j0},
\qquad 0\le t\le t_j^{\mathrm{hit}},
$$

其中

$$
t_j^{\mathrm{hit}}=\frac{\|M_{j0}\|_2}{v_M}.
$$

该式保证导弹的速度方向始终指向假目标，而非真目标。

### 4.2 无人机轨迹

定义水平单位航向向量

$$
h_i=(\cos\theta_i,\sin\theta_i,0),
$$

则无人机轨迹为

$$
U_i(t)=U_{i0}+v_it h_i,\qquad t\ge0.
$$

其 $z$ 坐标恒定。对同一无人机，$v_i$ 和 $\theta_i$ 对该题所有投弹保持一致。

### 4.3 投放后至起爆前的弹体轨迹

投放点为

$$
P_{ik}^{\mathrm r}=U_i(t_{ik}^{\mathrm r}).
$$

在假设 A1 下，$t\in[t_{ik}^{\mathrm r},t_{ik}^{\mathrm e}]$ 时

$$
B_{ik}(t)=P_{ik}^{\mathrm r}
+v_i(t-t_{ik}^{\mathrm r})h_i
-\frac12g(t-t_{ik}^{\mathrm r})^2e_z,
$$

其中 $e_z=(0,0,1)$。因此起爆点

$$
P_{ik}^{\mathrm e}=P_{ik}^{\mathrm r}
+v_i\delta_{ik}h_i
-\frac12g\delta_{ik}^2e_z.
$$

物理可行性至少要求

$$
t_{ik}^{\mathrm r}\ge0,\quad \delta_{ik}\ge0,\quad
(P_{ik}^{\mathrm e})_z\ge0.
$$

同一无人机按投放时刻排序后还需满足

$$
t_{i,k+1}^{\mathrm r}-t_{ik}^{\mathrm r}\ge1.
$$

### 4.4 起爆后的云团轨迹

在假设 A2 下，云团有效期内

$$
C_{ik}(t)=P_{ik}^{\mathrm e}-v_C(t-t_{ik}^{\mathrm e})e_z,
\qquad t\in[t_{ik}^{\mathrm e},t_{ik}^{\mathrm e}+T_C].
$$

有效烟幕区域为闭球

$$
\mathcal S_{ik}(t)=\{x\in\mathbb R^3:\|x-C_{ik}(t)\|_2\le R\}.
$$

## 5. 遮蔽几何：已冻结的模型选择

对任意目标点 $q\in\mathcal T$，导弹 $M_j(t)$ 到 $q$ 的视线线段为

$$
L_j(q,t)=\{M_j(t)+\lambda(q-M_j(t)):0\le\lambda\le1\}.
$$

云团中心到该线段的距离可稳定计算为

$$
\lambda^*=\operatorname{clip}_{[0,1]}
\frac{(C_{ik}(t)-M_j(t))^\top(q-M_j(t))}{\|q-M_j(t)\|_2^2},
$$

$$
d_{jik}(q,t)=\left\|C_{ik}(t)-\left[M_j(t)+\lambda^*(q-M_j(t))\right]\right\|_2.
$$

由此有三种不同语义：

1. **严格整目标遮蔽**：$\max_{q\in\mathcal T}d_{jik}(q,t)\le R$。即圆柱上每个可见点的视线都穿过有效烟幕球；最符合“导弹不能发现真目标”，计算最复杂。
2. **代表点中心线近似**：$d_{jik}(T_c,t)\le R$。简单、可快速得到问题 1 基准，但忽略圆柱有限尺寸。
3. **局部遮蔽**：$\min_{q\in\mathcal T}d_{jik}(q,t)\le R$。只需目标一小部分被遮住，通常过于宽松，不建议作为主判据。

题面不足以唯一选定三者。用户已确认以 1 为主模型、2 为近似基准并报告二者差异；3 不作为有效遮蔽口径。

### 5.1 严格判据的可计算形式

记同一时刻导弹位置为 $M$、云团中心为 $C$，$D=\|C-M\|_2$。当 $D>R$ 时，令

$$
a=\frac{C-M}{D},\qquad
m(q)=\left\|(q-M)-[(q-M)^\top a]a\right\|_2
-\frac{R}{\sqrt{D^2-R^2}}(q-M)^\top a.
$$

$m(q)\le0$ 表示目标点位于以 $M$ 为顶点、与烟幕球相切的视锥内。还需检查整个目标位于切平面后方，防止把烟幕球后侧延长锥中的点误算为遮蔽。由于 $m(q)$ 是凸函数，圆柱是凸集，其最大值可在圆柱的极点集合取得；该集合就是上、下底面的两条圆周。因此严格判据的连续三维极值可化为两个周期角度的一维最大化，而不是以有限目标点近似圆柱。

当 $D\le R$ 时导弹位于有效烟幕球内，所有从导弹出发的目标视线均与烟幕相交，直接判为有效遮蔽。这一分支对问题 1 的结束边界至关重要。

给定冻结后的瞬时遮蔽谓词 $I_{jik}(t)\in\{0,1\}$，单云团有效时间集合为

$$
E_{jik}=\{t:\ t_{ik}^{\mathrm e}\le t\le
\min(t_{ik}^{\mathrm e}+T_C,t_j^{\mathrm{hit}}),\ I_{jik}(t)=1\}.
$$

多云团对导弹 $j$ 的总有效时间必须按区间并集计算：

$$
D_j=\mu\left(\bigcup_{i,k}E_{jik}\right),
$$

其中 $\mu$ 是时间长度；重叠时段不得重复计时，不连续时段可以相加。

## 6. 问题 1 的固定输入

问题 1 中

$$
M_{10}=(20000,0,2000),\qquad U_{10}=(17800,0,1800).
$$

FY1 朝假目标飞行，因此

$$
h_1=(-1,0,0),\quad v_1=120,\quad
t_{11}^{\mathrm r}=1.5,\quad \delta_{11}=3.6,\quad
t_{11}^{\mathrm e}=5.1.
$$

可直接得到

$$
P_{11}^{\mathrm r}=(17620,0,1800),
$$

$$
P_{11}^{\mathrm e}=(17188,0,1800-6.48g).
$$

若采用 $g=9.8\ \mathrm{m/s^2}$，则起爆点为

$$
P_{11}^{\mathrm e}=(17188,0,1736.496).
$$

问题 1 的待求量是 $D_1=\mu(E_{111})$。第 5 节遮蔽语义已经冻结，数值实现与独立复算结果如下。

### 6.1 问题 1 数值结果

主值 $g=9.8\ \mathrm{m/s^2}$ 时：

- 投放点：$(17620,0,1800)$ m；
- 起爆点：$(17188,0,1736.496)$ m；
- 严格整圆柱遮蔽区间：$[8.0564454904,\ 9.4480881586]$ s；
- 严格有效遮蔽总时长：$1.3916426682$ s，比赛报告建议写作 **1.392 s**；
- 中心视线近似区间：$[8.0130060567,\ 9.4480881586]$ s，总时长 $1.4350821019$ s。

中心视线近似比严格判据多计 $0.0434394337$ s，即相对严格结果高约 $3.121\%$，主要来自把遮蔽开始时刻提前。严格区间结束时刻对应导弹离开烟幕球；此前导弹曾进入烟幕球，最近距离为 $4.7167067$ m。

采用 $g=9.81\ \mathrm{m/s^2}$ 时，起爆高度为 $1736.4312$ m，严格区间为 $[8.0431874897,\ 9.4482217034]$ s，总时长 $1.4050342137$ s；相对主值增加 $0.0133915455$ s（约 $0.962\%$）。

## 7. 五问依赖关系

| 问题 | 在前一阶段上增加的内容 | 依赖的冻结项 | 本轮状态 |
|---|---|---|---|
| 1 | 固定轨迹下计算单弹遮蔽区间 | 坐标、抛体、云团、遮蔽谓词、区间边界 | 已求解、验证并获用户验收 |
| 2 | 优化单机单弹的 $\theta_1,v_1,t^{\mathrm r},\delta$ | 问题 1 内核与数值容差 | 已求解、验证并获用户验收 |
| 3 | 同一固定航向/速度下协调 3 枚弹，处理 1 s 间隔和联合覆盖 | 问题 2 可行域、联合视线判据、Excel 模板 | 形式化已冻结，正在实现 |
| 4 | 三架无人机各一弹，允许不同航向/速度，合并遮蔽区间 | 问题 2 单弹变量、问题 1 区间算法、Excel 模板 | 未求解 |
| 5 | 5 架无人机、每架至多 3 弹、3 枚导弹的分配与联合调度 | 问题 3/4 约束、多导弹目标函数、Excel 模板 | 未求解 |

核心依赖顺序为：

$$
\text{几何判据}\rightarrow\text{问题 1 可复算基准}
\rightarrow\text{单弹优化}\rightarrow\text{区间并集与投放节拍}
\rightarrow\text{多机协同}\rightarrow\text{多导弹分配}.
$$

## 8. 需求追踪

| 题面要求或事实 | 模型元素 | 状态 | 依据/备注 |
|---|---|---|---|
| 导弹直指假目标 | $M_j(t)$ | 已表示 | 终点严格为 $O$ |
| 无人机瞬时转向后等高直线匀速 | $U_i(t)$ | 已表示 | 忽略转弯过程 |
| 无人机速度 70–140 m/s | $70\le v_i\le140$ | 已表示 | 问题 1 固定为 120 |
| 航向、速度确定后不再调整 | 每机共享 $(\theta_i,v_i)$ | 已表示 | 多弹时仍共享 |
| 脱离后受重力作用 | $B_{ik}(t)$ | 已表示 | 初始竖直速度待假设 A1 支持 |
| 起爆后瞬时成球 | $\mathcal S_{ik}(t)$ | 已表示 | 有效半径 10 m |
| 云团 3 m/s 下沉 | $C_{ik}(t)$ | 已表示 | 水平漂移未建模 |
| 起爆后 20 s 有效 | $E_{jik}$ 的时域 | 已表示 | 与导弹时域取交集 |
| 同机投弹至少间隔 1 s | 相邻投放时刻约束 | 已表示 | 仅问题 3、5 激活 |
| 多弹遮蔽可不连续 | 时间区间并集及测度 | 已表示 | 重叠不重复计时 |
| 对真目标有效遮蔽 | $I_{jik}(t)$ | 已冻结并实现 | 严格整圆柱为主、中心视线为对照 |
| 结果写入指定 Excel | 输出适配层 | **缺输入** | 三个模板尚未提供 |
| 问题 5 “尽可能长” | 多导弹目标函数 | **待确认** | 聚合口径题面不明确 |

## 9. 预期求解方法（仅问题 1）

1. 在云团有效时域上粗网格扫描遮蔽谓词，定位真假切换的候选小区间。
2. 对每个切换区间用一维有界求根或二分法确定边界。
3. 合并相邻或重叠区间，计算时间长度。
4. 将网格步长减半并收紧求根容差，确认有效时长稳定。
5. 若采用严格圆柱判据，内层需解圆柱表面最不利视线的几何极值；先验证其离散化结果，再考虑解析降维。

本阶段不指定公共库或优化算法，也不把网格分辨率当成模型假设。

## 10. 初始一致性检查

- 量纲：位置式各项均为 m；$v t$ 和 $g t^2$ 均为 m；距离阈值为 m；区间长度为 s。
- 符号：重力和云团下沉均沿 $-z$；FY1 朝原点时沿 $-x$。
- 边界：$\delta=0$ 时起爆点等于投放点；$t=t^{\mathrm e}$ 时云团中心等于起爆点；$t=t^{\mathrm e}+20$ 后不再有效。
- 线段约束：使用截断后的 $\lambda^*$，防止把无限直线上的近距离误判为遮蔽。
- 物理可行性：问题 1 起爆高度为正；后续优化必须显式限制起爆前不触地。
- 时间上界：$M_1$ 到达原点约需 $\sqrt{20000^2+2000^2}/300\approx67.0$ s，问题 1 的云团有效窗 $[5.1,25.1]$ s 完全位于其内。
- 可辨识性：问题 1 的运动学参数、遮蔽语义和重力口径均已冻结，结果唯一到声明的数值容差。

## 11. 问题 1 验证结论

- 解析轨迹检查给出投放点、起爆点、起爆时刻、云团寿命和导弹到达原点时刻，量纲、符号、时域与物理可行性均一致。
- 圆周初始离散从 256 增加至 4096 点时，严格时长在 $2\times10^{-15}$ s 内一致；离散只提供角度极值初值，最终边界由连续角度优化和 Brent 求根给出。
- 时间扫描步长从 0.1 s 减至 0.01 s 时，严格时长最大差约 $4.1\times10^{-11}$ s；扫描只用于寻找变号括区。
- 在区间起点前、起点、内部、终点、终点后进行状态检查，全部符合“外—边界—内—边界—外”。
- 独立对照没有使用切锥公式：对圆柱完整表面采样 349,920 点，直接计算云团中心到每条导弹—目标视线线段的距离；五个边界位置的判定与主实现一致。
- $g$ 敏感性和中心视线近似差异已经量化。完整记录见 `validation-q1.md` 与 `outputs/logs/q1_validation.json`。

上述证据支持问题 1 在已声明题意解释下“适用于当前用途，但有局限”：数值实现稳定且有独立几何对照；模型有效性仍依赖题面给定的球形均匀有效区以及无风、无扩散、瞬时成云等简化，不能由数值收敛本身证明。

## 12. 问题 2 优化规格

### 12.1 决策与输出

问题 2 使用 FY1 投放一枚烟幕弹干扰 M1。决策变量为

$$
x=(\theta,v,t^{\mathrm r},\delta),
$$

其中 $\theta\in[0,2\pi)$ 是从 $+x$ 轴逆时针计的水平航向角，$v\in[70,140]$ m/s 是无人机速度，$t^{\mathrm r}\ge0$ 是投放时刻，$\delta\ge0$ 是引信时长。允许 $t^{\mathrm r}=0$ 和 $\delta=0$，因为题面未规定最短反应时间或最短引信时间。

设 $h=(\cos\theta,\sin\theta,0)$、$t^{\mathrm e}=t^{\mathrm r}+\delta$，则所需策略输出由四个变量唯一导出：

$$
P^{\mathrm r}=U_{10}+vt^{\mathrm r}h,
$$

$$
P^{\mathrm e}=U_{10}+v(t^{\mathrm r}+\delta)h-\frac12g\delta^2e_z.
$$

这是因为弹体继承无人机水平速度，所以起爆水平位置等于无人机在 $t^{\mathrm e}$ 的水平位置。最终必须报告航向角（rad 和 deg）、速度、投放时刻与投放点、引信时长、起爆时刻与起爆点、严格遮蔽区间及总时长，并记录中心视线对照。

### 12.2 可行域

记 M1 到达假目标的时刻为

$$
t_1^{\mathrm{hit}}=\frac{\|M_{10}\|_2}{300}=66.9991708075\ \mathrm s.
$$

问题 2 的可行域为

$$
0\le\theta<2\pi,\quad70\le v\le140,
$$

$$
t^{\mathrm r}\ge0,\quad\delta\ge0,\quad
t^{\mathrm e}=t^{\mathrm r}+\delta\le t_1^{\mathrm{hit}},
$$

$$
1800-\frac12g\delta^2\ge0.
$$

在 $g=9.8$ 下，最后一个约束等价于 $\delta\le19.1663$ s。它保证弹体在触地前起爆。主模型按 D3 继续计算起爆后 20 s 的云团下沉轨迹；若最终计入遮蔽的时段出现云团中心 $z<0$，另做地面截断敏感性而不暗中改变主目标。

### 12.3 目标函数

对策略 $x$，云团有效候选时域为

$$
W(x)=[t^{\mathrm e},\min(t^{\mathrm e}+20,t_1^{\mathrm{hit}})].
$$

沿用问题 1 的严格整圆柱遮蔽谓词 $I_x(t)$，目标为

$$
\max_{x\in\mathcal X}D(x),\qquad
D(x)=\mu\{t\in W(x):I_x(t)=1\}.
$$

若单个云团产生多个不连续遮蔽区间，则 $D(x)$ 是全部区间并集的长度。中心视线时长只能用于候选生成和对照，不能决定最终策略排序。

### 12.4 求解方案

$D(x)$ 由遮蔽状态切换和区间边界决定，通常非凸、不可保证光滑，也可能随区间产生或消失而改变局部结构，因此不以单次梯度优化作为主方法。冻结的求解流程为：

1. 用中心视线近似进行宽范围、固定随机种子的候选搜索；
2. 对候选区域使用无梯度全局方法直接优化严格时长；
3. 对多个严格近优候选进行局部无梯度精修，防止只保留一个偶然盆地；
4. 最终统一以高精度严格内核复算、排序和生成区间；
5. 在最优点附近进行变量扰动、搜索预算/种子对照、时间与圆周精度收敛以及独立几何复核。

搜索阶段可采用较低的圆周初始采样数和较粗的变号扫描步长以控制成本，但必须保存该配置，且不得把搜索精度结果直接作为最终答案。

### 12.5 验收标准

- 最优候选满足全部速度、时间和起爆高度约束，导出点与运动学公式一致。
- 最终严格时长来自连续边界求根，不是网格点计数。
- 至少两个不同随机种子或一种替代搜索过程找到相同或足够接近的最优区域。
- 对最终候选提高时间和圆周精度后，报告精度内的策略价值稳定。
- 最优区间边界通过边界前后检查，并用直接视线线段距离作独立几何对照。
- 报告近优候选和局部扰动，区分“目标值稳定”与“决策变量唯一”。
- 明确记录最优遮蔽时段是否触发云团中心低于地面。

### 12.6 最佳已知策略与数值结果

在 $g=9.8\ \mathrm{m/s^2}$ 和严格整圆柱遮蔽口径下，当前搜索与独立边界精修得到的最佳候选为：

| 输出 | 数值 |
|---|---:|
| 航向角 | $3.0829669145$ rad，即 $176.6409926^\circ$ |
| 飞行速度 | $70.0000$ m/s |
| 投放时刻 | $0$ s |
| 投放点 | $(17800,0,1800)$ m |
| 引信时长 | $2.4967856396$ s |
| 起爆时刻 | $2.4967856396$ s |
| 起爆点 | $(17625.5252681,10.2404449,1769.4537012)$ m |
| 严格遮蔽区间 | $[2.4967876895,7.0396676367]$ s |
| 严格总时长 | $4.5428799472$ s |
| 中心视线总时长 | $4.5726737614$ s |

用于论文正文时建议报告严格时长为 **4.543 s**。该策略位于速度下界 $v=70$ 和投放时刻下界 $t^{\mathrm r}=0$；独立二维边界搜索使用不同随机种子，在固定这两个活跃边界后重新优化 $\theta$ 与 $\delta$，复现并改善了四维搜索候选。

### 12.7 问题 2 验证结论

- 四组时间扫描和圆周精度组合所得严格时长最大差为 $7.2\times10^{-12}$ s。
- 起爆前未激活、起爆后遮蔽开始前、区间起点、内部、终点及终点后的状态依次符合预期；起止裕量在声明容差内为零。
- 对圆柱完整表面独立采样 88,560 点，直接计算云团中心到导弹—目标视线线段的距离，全部边界状态与切锥主实现一致。
- 对航向正负扰动、速度增加、投放延迟以及引信时长正负扰动共六项局部检查，均未超过最佳候选时长；由于速度和投放时刻已在下界，只能对其作单侧扰动。
- 问题 1 策略经问题 2 通用内核复算，与已验收时长差 $1.78\times10^{-15}$ s。
- 最佳策略的云团中心最低高度为 $1709.4537$ m，未触发地下情形，因此 D3 的地面处理口径不影响问题 2 结果。
- 修复边界探针后，正式入口从配置、优化、验证到 manifest 的端到端运行成功；固定种子为 20250820。

全局和部分局部求解器因预算上限停止，日志状态为 `budget-exhausted-with-feasible-candidates`，因此本结果应表述为“经过多阶段搜索、独立边界精修和严格复算的最佳已知策略”，不能表述为已证明的全局最优。该限制不影响数值复算与局部稳定性结论。

## 13. 问题 2 检查点状态

问题 2 已由用户验收，其最佳已知策略可作为问题 3 的搜索热启动，但共享航向和速度在三弹联合优化中仍是决策变量，不预先固定为问题 2 的取值。

## 14. 问题 3 优化规格

### 14.1 联合覆盖语义

设时刻 $t$ 从 M1 出发且能够击中真目标的视线集合为 $\mathcal L_T(t)$，当时仍处于 20 s 有效期内的云团编号集合为 $\mathcal K(t)$。问题 3 的主遮蔽谓词冻结为

$$
I_{\mathrm{joint}}(t)=1
\iff
\forall \ell\in\mathcal L_T(t),\
\exists k\in\mathcal K(t):
\ell\cap\mathcal S_k(t)\ne\varnothing,
$$

且交点必须位于导弹和目标之间。该定义允许不同云团分别覆盖目标视域的不同部分，符合多烟幕联合遮蔽的物理语义。

若 $I_k(t)$ 表示第 $k$ 个云团单独完整遮住整个圆柱，则

$$
I_{\mathrm{conservative}}(t)=\max_{k=1,2,3}I_k(t)
$$

只是保守对照。量词关系为

$$
\exists k\,\forall\ell
\Longrightarrow
\forall\ell\,\exists k,
$$

反向一般不成立。问题 3 的最终排名必须使用 $I_{\mathrm{joint}}$，不得使用三个单云严格区间的简单并集替代。

在计算上，可令 $m_k(\ell,t)$ 为视线关于第 $k$ 个有效云团的带符号遮蔽裕量，则

$$
G(t)=\max_{\ell\in\mathcal L_T(t)}
\min_{k\in\mathcal K(t)}m_k(\ell,t),
$$

联合有效当且仅当 $G(t)\le0$。由于多个切锥的并集不是凸集，$\min_k m_k$ 不再具有问题 1 中可直接利用的凸极值结构，不能只检查两条圆周上的单个凸函数最大值；正式计算必须检查目标视域内部是否存在未覆盖小孔。

### 14.2 决策变量与投弹间隔

三枚弹共享无人机航向 $\theta$ 和速度 $v$。为自动保证投放顺序及至少 1 s 的间隔，引入非负等待变量 $s_0,s_1,s_2$：

$$
t_1^{\mathrm r}=s_0,
$$

$$
t_2^{\mathrm r}=s_0+1+s_1,
$$

$$
t_3^{\mathrm r}=s_0+2+s_1+s_2.
$$

每枚弹具有独立引信时长 $\delta_k\ge0$。因此决策向量为

$$
x=(\theta,v,s_0,s_1,s_2,\delta_1,\delta_2,\delta_3).
$$

其中 $0\le\theta<2\pi$、$70\le v\le140$。第 $k$ 枚弹的起爆时刻、投放点和起爆点分别为

$$
t_k^{\mathrm e}=t_k^{\mathrm r}+\delta_k,
$$

$$
P_k^{\mathrm r}=U_{10}+vt_k^{\mathrm r}h,
$$

$$
P_k^{\mathrm e}=U_{10}+vt_k^{\mathrm e}h
-\frac12g\delta_k^2e_z.
$$

可行性要求

$$
t_k^{\mathrm e}\le t_1^{\mathrm{hit}},\qquad
1800-\frac12g\delta_k^2\ge0,\qquad k=1,2,3.
$$

题面只规定投放间隔，没有规定起爆顺序，因此不增加 $t_1^{\mathrm e}\le t_2^{\mathrm e}\le t_3^{\mathrm e}$ 约束。三枚弹按投放时刻编号，避免标签置换产生重复解。

### 14.3 目标函数与贡献分解

问题 3 的联合有效时间集合及目标为

$$
E_{\mathrm{joint}}(x)
=\{t\in[0,t_1^{\mathrm{hit}}]:I_{\mathrm{joint}}(t)=1\},
$$

$$
\max_{x\in\mathcal X}D_3(x),\qquad
D_3(x)=\mu(E_{\mathrm{joint}}(x)).
$$

$E_{\mathrm{joint}}$ 可以包含多个不连续区间，必须以连续边界求根后合并区间，不能用有效网格点数或三枚弹时长之和代替。除总时长外，还应报告：

- 每枚弹单独完整遮蔽的区间；
- 单云严格区间并集形成的保守下界；
- 三云联合覆盖新增的时长；
- 删除每一枚弹后的联合时长及边际贡献；
- 三枚弹之间的时间重叠和冗余。

### 14.4 求解方案

冻结的搜索流程为：

1. 以问题 2 最佳候选和满足 1 s 间隔的顺序构造方案作为可行热启动；
2. 在共享 $(\theta,v)$ 下逐弹寻找当前未覆盖时段，建立非零联合覆盖候选；
3. 使用中心视线或低分辨率目标视域代理进行八维广域搜索；
4. 用联合严格裕量直接优化多个候选；
5. 对候选进行无梯度局部精修，最后统一以正式目标视域与时间精度复算；
6. 将保守单云区间并集作为下界，而非主目标。

搜索代理必须保留连续几何引导，避免八维随机种群全部落入零时长平台。所有随机种子、初始构造、预算、终止状态和精度配置必须写入求解日志。

### 14.5 验收标准

- 三枚弹共享完全相同的航向和速度，相邻投放时刻差均不小于 1 s。
- 每枚弹起爆时刻不晚于 M1 命中时刻，且在触地前起爆。
- 单云输入时联合实现严格回归问题 1、2；调换云团标签不改变联合判定和总时长。
- 联合时长不小于保守单云严格区间并集，且两者差值被明确报告。
- 提高目标视域采样、自适应细化和时间扫描精度后，联合区间及总时长在报告精度内稳定。
- 在联合遮蔽边界前后检查最坏未覆盖视线，并用直接视线—球相交计算作独立对照。
- 人为构造“单云均不能完整覆盖、但多云联合能够完整覆盖”的互补案例，防止实现退化为保守口径。
- 删除任意一枚弹后的边际贡献、云团落地状态及近优策略均有记录。
- 数值搜索未满足全局收敛条件时，只报告最佳已知策略，不宣称全局最优。

### 14.6 输出适配

官方 result1.xlsx 当前缺失。正式运行先生成带 provisional-schema 标记的临时 outputs/tables/result1.xlsx，每枚弹一行，同时保留 CSV、联合区间表、求解日志和验证日志。获得官方模板后只增加字段映射，不重新求解。

## 15. 问题 3 最佳已知策略与验证

在 $g=9.8\ \mathrm{m/s^2}$ 和联合覆盖口径下，当前严格复算的最佳已知策略为：

| 输出 | 数值 |
|---|---:|
| 共享航向角 | $3.1047922066$ rad，即 $177.8914897^\circ$ |
| 共享速度 | $75.5374085$ m/s |
| 投放时刻 | $(0.0000866,\ 1.0001937,\ 2.0002754)$ s |
| 引信时长 | $(2.7648584,\ 2.5816390,\ 9.3343535)$ s |
| 起爆时刻 | $(2.7649450,\ 3.5818327,\ 11.3346290)$ s |
| 联合遮蔽区间 | $[2.7649450441,\ 8.2785964073]$ s |
| 联合总时长 | $5.5136513631$ s |
| 保守单云区间并集 | $5.5136513627$ s |

用于论文正文时建议报告为 **5.514 s**。第一、二枚弹的单独严格遮蔽时长分别为 $4.1487610$ s 和 $3.2355763$ s；第三枚弹的单独时长为 $0$，删除它后的时长变化仅 $6.2\times10^{-13}$ s。因此这个候选实质由前两枚弹完成，不应声称已充分利用三枚弹。

联合时长与保守并集在该候选上仅差 $4.1\times10^{-10}$ s，但人工互补案例已验证实现确实支持“每朵云都不完整、联合后完整”，并未退化成保守口径。

### 15.1 数值证据与结论边界

- 三组时间—表面精度下的总时长最大差为 $3.25\times10^{-13}$ s。
- 相邻投放间隔为 $1.0001071$ s 和 $1.0000818$ s，高度、起爆时刻及其他物理约束均可行。
- 在区间起点、中点和终点，110,160 条独立二次方程视线检查均覆盖；起点前和终点后均未覆盖。
- 六种云团标签排列的时长完全一致；单云回归问题 2 的误差为 $5.64\times10^{-11}$ s。
- 搜索中发现并修复了候选相关时间网格造成的伪增益；修正后完整八维搜索使用绝对时间 $t=0$ 锚定网格重跑，并保留严格复算更优的已知可行解。
- 全局和局部求解均达到预算上限，所以只能称“最佳已知策略”，不能称全局最优。修正后全搜索也未找到第三枚弹的实质贡献，这是进入问题 4 前需保留的算法局限。

## 16. 问题 3 检查点状态

问题 3 的实现、求解与验证已完成；当前继续推进问题 4。

## 17. 问题 4 优化规格

### 17.1 对象、变量与动力学

问题 4 仅考察导弹 M1，使用无人机集合 $\mathcal I_4=\{FY1,FY2,FY3\}$，每架无人机各投放一枚烟幕弹。对每架无人机 $i$，决策变量为

$$
x_i=(\theta_i,v_i,t_i^{\mathrm r},\delta_i),
$$

其中 $\theta_i\in[0,2\pi)$、$v_i\in[70,140]\ \mathrm{m/s}$、$t_i^{\mathrm r}\ge0$、$\delta_i\ge0$。全部决策向量 $x=(x_i)_{i\in\mathcal I_4}$ 共 12 维。

记初始位置为 $U_{i0}$，水平单位航向为 $h_i=(\cos\theta_i,\sin\theta_i,0)$，则

$$
P_i^{\mathrm r}=U_{i0}+v_it_i^{\mathrm r}h_i,
$$

$$
t_i^{\mathrm e}=t_i^{\mathrm r}+\delta_i,
$$

$$
P_i^{\mathrm e}=U_{i0}+v_it_i^{\mathrm e}h_i-\frac12g\delta_i^2e_z.
$$

三架无人机的航向和速度互相独立，且确定后均不再改变。因每朶只投一弹，同机相邻投弹间隔约束不激活。

### 17.2 可行域与目标

每枚弹要求在触地前、且不晚于 M1 到达假目标时起爆：

$$
t_i^{\mathrm e}\le t_1^{\mathrm{hit}},\qquad
z_{i0}-\frac12g\delta_i^2\ge0.
$$

起爆后云团按公共动力学下沉并有效 20 s。联合判定完全沿用问题 3：对 M1 到真目标的每条视线，至少有一朵当前有效的云在目标前与其相交。目标函数为

$$
\max_{x\in\mathcal X_4}
D_4(x),\qquad
D_4(x)=\mu\{t\in[0,t_1^{\mathrm{hit}}]:I_{\mathrm{joint}}(t;x)=1\}.
$$

不使用三枚弹单独时长之和，也不对重叠区间重复计时。

### 17.3 求解路线

1. 分别对 FY1、FY2、FY3 做单机单弹粗搜索，得到可行遮蔽时窗和几何方向；
2. 以单弹候选的接力、重叠和视域互补组合构造 12 维种群；
3. 用绝对任务时间 $t=0$ 锚定的低分辨率联合覆盖代理进行广域搜索；
4. 对多个可行候选进行无梯度局部精修，最后以自适应目标表面证书和连续边界求根统一复算；
5. 保留问题 3 已验证几何内核的单云回归、标签置换、边界及互补覆盖检查。

搜索必须保存固定种子、预算终止状态、候选集和正式复算精度。既有问题 3 最佳策略不是问题 4 的可行下界，因为它的三枚弹来自同一架无人机；问题 4 必须使用指定的三架无人机各一弹。

### 17.4 验收与输出

- 策略严格包含 FY1、FY2、FY3 各一弹，且各自共享自己的固定航向和速度。
- 所有弹的投放、起爆时刻、起爆高度与速度范围可行。
- 联合时长不小于任一单云遮蔽时长，并报告删除每朵云的损失。
- 精度收敛、边界、独立视线球交、云标签置换和问题 2/3 内核回归通过。
- 由于官方 `result2.xlsx` 缺失，暂时生成每架无人机一行并带 `provisional-schema` 标记的工作簿；获得模板后只做字段映射。

### 17.5 最佳已知策略

在固定种子 20250904 和正式联合判定下，当前最佳已知联合时长为 **10.1388573260 s**，由三段不连续区间组成：

$$
[5.1449298089,9.5689345086],
$$

$$
[12.9484732632,16.4222046726],
$$

$$
[42.4765836426,44.7177048596].
$$

| 无人机 | 航向角 | 速度 / m/s | 投放时刻 / s | 引信 / s | 起爆时刻 / s | 起爆点 / m |
|---|---:|---:|---:|---:|---:|---:|
| FY1 | $179.301490^\circ$ | 136.942948 | 0.815997 | 3.963735 | 4.779732 | $(17145.498,7.980,1723.015)$ |
| FY2 | $290.170336^\circ$ | 112.126363 | 7.471102 | 5.477371 | 12.948473 | $(12500.621,37.175,1252.992)$ |
| FY3 | $120.730428^\circ$ | 138.527165 | 19.053573 | 6.818714 | 25.872287 | $(4168.570,80.751,472.175)$ |

三朵云的删除损失依次为 $4.4239346$ s、$3.4736560$ s 和 $2.2411125$ s，因此三枚弹均有实质贡献。当前策略主要表现为三段单云接力，未产生可报告的视域互补额外时长。

### 17.6 验证结论

- 三档时间—表面精度所得总时长的最大差为 $1.15\times10^{-4}$ s，足以支持毫秒级报告。
- 三架无人机的速度、投放时刻、起爆时刻和起爆高度均可行；云团最低中心高度仍为 $412.175$ m。
- 每段区间均通过边界前后探针和 110,160 条独立单位方向视线球交检查。单点边界的开闭浮点差异不改变区间测度。
- 六种云团标签排列时长一致，问题 3 几何回归误差为 0，互补覆盖构造案例通过。
- DE 与 Nelder–Mead 均因预算停止，日志明确 `convergence_claimed=false`；该结果是最佳已知策略，不是全局最优证明。

## 18. 问题 5 优化规格

### 18.1 集合、运动学与决策变量

记无人机集合 $\mathcal I_5=\{FY1,\ldots,FY5\}$，导弹集合 $\mathcal J=\{M1,M2,M3\}$，每架无人机最多投放 3 枚弹。对无人机 $i$，航向 $\theta_i$ 和速度 $v_i$ 为该机全部弹共享的飞行决策。三枚弹的投放时刻仍以非负等待变量参数化：

$$
t_{i1}^{\mathrm r}=s_{i0},\quad
t_{i2}^{\mathrm r}=s_{i0}+1+s_{i1},\quad
t_{i3}^{\mathrm r}=s_{i0}+2+s_{i1}+s_{i2},
$$

并为每枚弹设置独立引信 $\delta_{ik}\ge0$。因题面不对弹药消耗加惩罚，多一朵可行云不会缩小任一导弹的遮蔽时刻集合，所以求解阶段可将“至多 3 枚”等价扩展为每机 3 个可行弹位；若某弹删除损失为数值零，输出时可标记为未使用。总计 40 个连续变量。

弹体和云团运动学沿用问题 3、4，但视点替换为各导弹自身的位置 $M_j(t)$。导弹命中假目标的时刻分别约为

$$
(t_1^{\mathrm{hit}},t_2^{\mathrm{hit}},t_3^{\mathrm{hit}})
=(66.9992,63.7504,60.3665)\ \mathrm{s}.
$$

### 18.2 三导弹联合遮蔽与主目标

对每枚导弹 $j$，单独计算所有有效云团对其真目标视域的 $\forall\ell\exists k$ 联合覆盖区间 $E_j(x)$，并令

$$
D_j(x)=\mu(E_j(x)).
$$

题面只说对三枚导弹实施干扰，未明示总时长的聚合方式。主模型采用可加的总遮蔽导弹—秒数：

$$
\max_{x\in\mathcal X_5}J_{\mathrm{sum}}(x),\qquad
J_{\mathrm{sum}}(x)=\sum_{j\in\mathcal J}D_j(x).
$$

该口径把对不同导弹同时遮蔽视为两份有效干扰，但对同一导弹的重叠云团仍不重复计时。为防止总和掩盖局部失衡，同时报告

$$
J_{\min}(x)=\min_{j\in\mathcal J}D_j(x)
$$

和三枚导弹的各自时长。若后续确认应使用最差导弹优先口径，不需改变几何内核，只需将候选排序改为字典序 $(J_{\min},J_{\mathrm{sum}})$。

### 18.3 分层求解

40 维上直接使用一次差分进化既难以覆盖、又会在联合几何判定上产生不可控的评估量。因此采用分层与块坐标策略：

1. 对 5 架无人机—3 枚导弹的 15 个组合生成单弹可行候选库，记录航向、速度和有效时窗；
2. 对每架无人机从候选库选择一组共享 $(\theta_i,v_i)$，再在该航线上优化 3 枚弹的投放—引信时序；
3. 以导弹分配、时间接力和视域互补构造多个 40 维可行策略，按 $(J_{\mathrm{sum}},J_{\min})$ 排名；
4. 交替固定其他无人机，对单架无人机的 8 维变量进行有预算的块坐标精修；
5. 对前若干候选用正式目标表面分辨率和每枚导弹的连续时间根统一复算。

粗搜索时仍使用绝对任务时间 $t=0$ 锚定网格，不允许候选寿命改变早期采样相位。必须保留候选库、分配、每轮块精修、固定种子、预算停止状态和正式复算精度。

### 18.4 验收与输出

- 每架无人机至多 3 枚弹，同机相邻投放间隔不小于 1 s，且同机所有弹共享完全相同的航向和速度。
- 所有使用弹在触地前起爆；时间窗按各导弹自身命中时刻截断。
- 分别报告 $D_1,D_2,D_3$、$J_{\mathrm{sum}}$、$J_{\min}$、每朵云的删除贡献和每导弹的区间。
- 对每枚导弹完成精度收敛、边界前后、独立视线球交及标签排列检查，并回归 M1 上的问题 4 几何结果。
- 求解器未全局收敛时只称最佳已知策略；总和与最差时长的口径差异必须在论文限制中声明。
- 官方 `result3.xlsx` 缺失，先生成每个弹位一行的 `provisional-schema` 工作簿，获得模板后只做字段映射。

### 18.5 最佳已知策略

分层搜索并严格复算得到

$$
(D_1,D_2,D_3)
=(10.2500381747,1.9128520514,1.7411337307)\ \mathrm{s},
$$

$$
J_{\mathrm{sum}}=13.9040239568\ \mathrm{s},\qquad
J_{\min}=1.7411337307\ \mathrm{s}.
$$

五架无人机各有一枚弹的删除损失为正，其余 10 个弹位的删除损失为数值零，因此最终按“至多 3 枚”将它们标记为未使用。实际使用策略为：

| 无人机 | 航向角 | 速度 / m/s | 投放 / s | 引信 / s | 起爆 / s | 起爆点 / m |
|---|---:|---:|---:|---:|---:|---:|
| FY1 | $179.301490^\circ$ | 136.942948 | 0.815997 | 4.013282 | 4.829279 | $(17138.713,8.062,1721.078)$ |
| FY2 | $290.170336^\circ$ | 112.126363 | 7.471102 | 5.477371 | 12.948473 | $(12500.621,37.175,1252.992)$ |
| FY3 | $121.013390^\circ$ | 138.340570 | 19.098230 | 6.946564 | 26.044794 | $(4143.569,87.984,463.552)$ |
| FY4 | $303.831378^\circ$ | 118.628302 | 6.375676 | 9.749407 | 16.125083 | $(12065.003,411.000,1334.250)$ |
| FY5 | $135.527992^\circ$ | 114.522224 | 15.757616 | 4.584975 | 20.342591 | $(11337.558,-367.919,1196.992)$ |

M1 的区间为 $[4.9626150,9.4278446]$、$[12.9484733,16.4222047]$、$[40.9072599,43.2183371]$ s；M2 的区间为 $[16.1250831,18.0379351]$ s；M3 的区间为 $[20.3425906,22.0837243]$ s。

### 18.6 搜索证据与限制

- 15 个无人机—导弹组合均保存候选库记录，其中 13 个在当前低预算搜索中找到正遮蔽；FY1–M2 和 FY1–M3 只能记为“当前预算未找到”，不是物理不可行证明。
- 搜索始终保留问题 4 在 M1 上的三云可行下界；最终 $D_1=10.2500382$ s 不低于该下界 $10.1388573$ s。
- 针对其余弹位另做了 $5\times16$ 次评估的边际填充搜索。其新候选严格目标为 $13.8693042$ s，低于已知 $13.9040240$ s，因此未替换 persistent incumbent。
- 三枚导弹的三档精度跨度分别为 $2.13\times10^{-4}$、$6.90\times10^{-5}$、$9.05\times10^{-8}$ s；每个区间的中点、起点前和终点后均通过 44,280 条独立视线检查。
- 云团标签排列抽样的时长偏差为 0，问题 4 在 M1 上的几何回归误差为 $9.92\times10^{-10}$ s，$J_{\mathrm{sum}}$ 算术一致。
- 本问是分层、有预算的启发式搜索，未对 40 维混合非光滑目标给出全局最优证明。主目标采用总导弹—秒数，因此结果向 M1 倾斜；$J_{\min}$ 仅为 $1.7411$ s，这一不均衡性必须在论文中明示。

## 19. 项目建模状态

五个问题的形式化、数值实现、正式结果和分问验证均已完成。问题 1–5 已写入 `paper-draft.md`，终稿已完成符号、数值、结论边界和附件索引复核，未保留问题占位符。
